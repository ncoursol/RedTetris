# RedTetris Setup
```
pnpm init
pnpm install express
npx @vue/cli create front
pnpm install socket.io
pnpm install cors
```

Front:
```
pnpm install
pnpm install vuex
```

# Run App

Back:
```
pnpm start
```
Front:
```
cd front
pnpm run serve
```